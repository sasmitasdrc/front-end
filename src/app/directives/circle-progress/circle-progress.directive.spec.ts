import { CircleProgressDirective } from './circle-progress.directive';

describe('CircleProgressDirective', () => {
  it('should create an instance', () => {
    const directive = new CircleProgressDirective();
    expect(directive).toBeTruthy();
  });
});
