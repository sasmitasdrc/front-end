import { SpiderchartDirective } from './spiderchart.directive';

describe('SpiderchartDirective', () => {
  it('should create an instance', () => {
    const directive = new SpiderchartDirective();
    expect(directive).toBeTruthy();
  });
});
